# R23 에러·빈 상태·삭제 안전장치

## 파일 (각 `.test.tsx` 먼저)
- `components/dashboard/templates-list.tsx` — 테스트: `AlertDialog` 존재, `templates.delete.confirmTitle` 존재.
  카드 삭제 클릭 시 AlertDialog: 제목 `templates.delete.confirmTitle`("'{{name}}' 템플릿을 삭제할까요?"), 설명 `templates.delete.confirmDescription`("템플릿으로 만든 문서는 남고, 템플릿만 사라집니다."), 확인 `templates.delete.confirm`("템플릿 삭제")(`text-destructive`), 취소 `common.cancel`. 성공 토스트 `templates.delete.success`("템플릿을 삭제했어요"). 실패 토스트 `templates.delete.error`("템플릿을 삭제하지 못했습니다. 다시 시도해 주세요."). 발행 성공 토스트는 `templates.publish.success`("발행했어요. 링크를 공유해 보세요.").
- `components/dashboard/bulk-delete-modal.tsx` — 테스트: `dashboard.bulkDelete.andMore` 존재, `items` prop 존재.
  props 에 `items: { id: string; name: string; status: string }[]` 추가. 본문: `dashboard.bulkDelete.modalWarning`(값 "선택한 문서 {{count}}개를 삭제할까요?") + 상위 3개 이름 목록(`ul list-disc`) + 4개 이상이면 `dashboard.bulkDelete.andMore`("외 {{count}}개") + 초안이 있으면 `dashboard.bulkDelete.draftWarning`, 완료 문서가 있으면 `completedWarning`(기존 키 값 사용). 확인 버튼 `dashboard.bulkDelete.confirmDelete` 값 "{{count}}개 삭제".
- `components/dashboard/dashboard-content.tsx` — 테스트: `failedIds` 문자열 존재, `Error:` 문자열 부재.
  일괄 삭제 실패 매칭을 id 기준으로(`failedIds: Set<string>`). 순차 루프는 유지하되 진행 표시 `dashboard.bulkDelete.progress`("{{done}}/{{total}} 삭제 중…") 를 모달 버튼 라벨로. 에러 상태 UI: `Error: {error}` 를 `dashboard.error.load`("문서를 불러오지 못했습니다.") + `common.retry`("다시 시도") 버튼(재조회 호출)으로. `setError("Failed to load dashboard data")` → `setError(t("dashboard.error.load"))`.
- `components/dashboard/publications-list.tsx`, `components/dashboard/templates-list.tsx` — 동일한 에러 UI(각각 `dashboard.publications.error.load`("발행 목록을 불러오지 못했습니다."), `templates.error.load`("템플릿을 불러오지 못했습니다.")) + 재시도. 이름 매칭 → id 매칭.
- `components/dashboard/publication-card.tsx` — 테스트: `alert(` 부재. `alert()` 2곳을 `toast.error(t("dashboard.publications.delete.error"))`("발행을 삭제하지 못했습니다. 다시 시도해 주세요.") 로. 영어 하드코딩 제거.
- `components/delete-document-modal.tsx` — 테스트: `[가-힣]` 정규식으로 소스에 한글 부재. 문구를 키로: `documentDetail.delete.title`("문서 삭제"), `documentDetail.delete.confirm`("'{{name}}' 문서를 삭제할까요?"), `documentDetail.delete.description`("문서와 지정한 서명 칸이 함께 삭제되며 되돌릴 수 없습니다."), `common.cancel`("취소"), `common.deleting`("삭제 중…"), `common.delete`("삭제").
- 빈 상태 카피(locale 값만 변경): dashboard.empty.title="서명받을 문서를 올려보세요", dashboard.empty.description="문서를 올리고 서명 위치만 찍으면, 링크 하나로 서명을 받을 수 있어요.", dashboard.empty.action="문서 올리기"; dashboard.publications.empty.title="아직 보낸 문서가 없어요", description="초안을 발행하면 서명 링크가 만들어집니다.", action="초안 발행하기"; templates.empty.title="반복해서 쓰는 문서가 있나요?", description="한 번 저장해 두면 매번 서명 칸을 다시 잡지 않아도 됩니다.", action="첫 템플릿 만들기". en 은 대응 영어.

## 공통 키 (없으면 추가)
common.cancel, common.retry, common.delete, common.deleting.

## 수용 기준
- `npx vitest run`, `npx tsc --noEmit` 통과
- `grep -rn "alert(" components app --include=*.tsx` 0건
- `grep -rn "Error: {" components/dashboard` 0건
- `grep -c "[가-힣]" components/delete-document-modal.tsx` 0
