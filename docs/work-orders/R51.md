# R51 요금제 페이지 리뉴얼

## 파일
- `app/(billing)/pricing/components/PricingPage.tsx` (+test 먼저: `alert(` 부재, `연동 예정` 부재(ko.ts 도 확인), `role="radiogroup"` 존재, `bg-green-`·`bg-red-500`·`text-red-600`·`scale-105` 부재, 인라인 한글 폴백 `t("` 뒤 `"` `,` `"[가-힣]` 패턴 부재)
- 순수 로직 분리 `lib/paddle/plan-cta.ts` (+test): `resolvePlanCta({ planKey, isCurrent, isFree, isEnterprise, currentRank, planRank }) → { labelKey, href | null, disabled }`: 현재 플랜 → `pricingPage.currentlyUsing` disabled; 엔터프라이즈 → `pricingPage.contactUs` → /contact; 무료이고 상위 플랜 사용 중 → `pricingPage.downgradeNotAllowed`("다운그레이드 불가") disabled; 그 외 → `pricingPage.selectPlan`(값 "이 플랜으로 변경") 활성.

## 스펙
- 헤더(spec 패턴): h1 `pricingPage.title` 값 "요금제", 설명 로그인 시 `pricingPage.currentPlan` 값 "현재 {{planName}} 플랜 · 이번 달 {{used}}/{{limit}}건 보냄" (사용량 데이터가 없으면 플랜명만), 비로그인 시 `pricingPage.subtitle`("플랜은 한 달에 보내는 문서 수로만 나뉩니다."). 오른쪽 월간/연간 토글 `role="radiogroup"` + 각 버튼 `role="radio" aria-checked` + `focus-visible:ring-2`.
- 카드 3열 `grid gap-6 md:grid-cols-3`, `rounded-xl border bg-card p-7 flex flex-col gap-5`, 현재 플랜은 `border-primary shadow-md` + 좌상단 `-top-3 left-6` 배지 `pricingPage.currentBadge`; 인기 플랜은 `pricing.popular` 배지(둘 다면 현재 플랜만). `scale-105`·translate 배지 제거.
- 가격 `text-4xl font-bold tabular-nums` + `/월`·`/년`. 기능 목록 Check 아이콘 `text-primary`.
- 플랜 선택 실패·성공 안내는 `toast`(sonner). `alert()` 제거. `pricingPage.alertMessage` 키와 값("결제 모듈 연동 예정입니다") 삭제(ko/en).
- 추가문서 섹션: 카드 1개 `flex flex-col md:flex-row md:items-center justify-between gap-4`: 제목 `pricing.credit.title`, 설명 `pricing.credit.description`, 가격 행, 버튼 `pricing.credit.buy`. 인라인 한글 폴백 문자열은 전부 키로 옮기고 값에서 "크레딧" → "추가문서".
- 로딩은 스켈레톤 카드 3개(`animate-pulse rounded-xl border bg-card h-[420px]`), 스피너 제거. 에러 상태 `pricingPage.errorTitle` 값 "요금제를 불러오지 못했습니다" + `common.retry`.
- 하드코딩 색 → 토큰(`bg-seal-soft text-seal` 는 할인 배지에만 허용).

## locale
값 변경: pricingPage.title, pricingPage.currentPlan, pricingPage.selectPlan, pricingPage.errorTitle, pricingPage.subtitle, pricing.limitPerMonth 는 홈과 동일("월 {{count}}건 문서 발송"). 추가: pricingPage.downgradeNotAllowed, pricing.credit.buy 등 필요한 키. 삭제: pricingPage.alertMessage. en 대응.

## 수용 기준
- `npx vitest run`, `npx tsc --noEmit` 통과
- `grep -rn "연동 예정\|크레딧" locales app components --include=*.ts --include=*.tsx` 0건 (서버 액션 파일 제외: `app/actions/**` 는 R61 범위)
